# functions/.env — populated after terraform apply + key creation
# These are loaded by Firebase Functions at runtime (NOT committed to git)
GCS_CLIENT_EMAIL=citigrove-media-reader@organic-spirit-488116-e2.iam.gserviceaccount.com
GCS_BUCKET_NAME=citigrove-images
# GCS_PRIVATE_KEY is stored in Firebase Secret Manager (set via: firebase functions:secrets:set GCS_PRIVATE_KEY)
